import React from 'react';
import TodoList from './TodoList';

const PublicTodos = () => {
  return (
    <div>
      <h2>Public Todos</h2>
      <TodoList />
    </div>
  );
};

export default PublicTodos;
